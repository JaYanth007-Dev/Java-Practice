Reapeat function
  String j="Jay".repeat(10);


  // Trim and Strip
  // Strip is advanced version od trim,trim is used to trim only ascii charecters and strip will work for all (language) characters
  String j="Jay   ".trim();
  String k="Kumar ".strip();
